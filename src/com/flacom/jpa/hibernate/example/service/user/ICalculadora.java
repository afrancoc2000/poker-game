package com.flacom.jpa.hibernate.example.service.user;

import com.flacom.jpa.hibernate.example.domain.user.Operador;

public interface ICalculadora {
     Operador calcularEstimacion(Operador operadores);
}
